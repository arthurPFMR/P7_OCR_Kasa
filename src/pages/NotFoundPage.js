import React from "react";
import Error from "../components/Error";

const NotFoundPage = () => {
  return (
    <div className="DOM">
      <Error />
    </div>
  );
};

export default NotFoundPage;
